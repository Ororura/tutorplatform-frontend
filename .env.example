# Server-side target used by the Next.js /api rewrite. Browser requests stay same-origin.
BACKEND_INTERNAL_URL=http://localhost:8080

# Development-only login form helpers. Ignored by production builds even if set to true.
NEXT_PUBLIC_DEMO_MODE=false
